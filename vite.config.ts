import { defineConfig } from 'vite'

// GitHub Pages (project page) 用のベースパス設定
// リポジトリ名が ultra_warp_ride.io のため、ベースは "/ultra_warp_ride.io/"
export default defineConfig({
  base: '/ultra_warp_ride.io/',
})

